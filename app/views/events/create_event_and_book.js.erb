<% if @teacher.instance_of? Array %>
  $('.modal-body').prepend("<%= escape_javascript(@teacher[0].to_s) %>");
<% else %>
  $('.modal-body').empty();
  $('.modal-body').append("<%= escape_javascript( render 'partials/payment_buttons_for_user', object: @teacher ) %> ");
<% end %> 
